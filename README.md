# GitHub Release Notifier

Професійний мікросервіс для відстеження релізів у GitHub репозиторіях з автоматичним email-сповіщенням.

## Особливості (Features)
* **API на Go (Chi):** Швидкий та надійний HTTP-сервер.
* **PostgreSQL:** Повноцінна база даних для зберігання підписок.
* **Автоматичні міграції:** База налаштовується сама при старті (golang-migrate).
* **Фоновий сканер:** Автономно перевіряє GitHub кожні 5 хвилин (Goroutines).
* **Security:** Доступ до API захищений через **API-Key** (Custom Middleware).
* **Docker & Compose:** Запуск всього проєкту однією командою.
* **CI/CD:** Налаштовані GitHub Actions для автоматичного тестування.
* **Documentation:** Додано Swagger (OpenAPI) опис у папці `docs`.

## Швидкий старт (Локально)

1. Переконайтеся, що у вас встановлено **Docker Desktop**.
2. Склонуйте проєкт та виконайте в терміналі:
   ```bash
   docker-compose up --build
   ```
3. Сервер запуститься на порту `8081`.

## Авторизація
Для виконання запитів до `POST /api/subscribe` необхідно передавати заголовок:
`X-API-Key: super-secret-key-123`

## Тестування
Проєкт покритий Table-Driven юніт-тестами. Запуск:
```bash
go test -v ./internal/api/...